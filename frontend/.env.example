# Backend API base URL — MUST include /api suffix
# Local dev:   http://localhost:3001/api   (Vite proxy also works with just /api)
# Production:  https://your-backend.railway.app/api  ← do NOT omit /api
VITE_API_URL=http://localhost:3001/api
